# Development

Обновлено: 2026-09-16.

Единый рабочий план OmniList: важные требования, состояние реализации и следующие задачи. Пересмотрен по предоставленной пользователем стратегии `omnilist-product-strategy.md` от 2026-09-13 и текущему коду. Остальные MD сохраняются как история, а не конкурирующие планы. Конкурентные оценки из стратегии являются гипотезами, не доказанными преимуществами.

## 1. Продукт и границы

**Создать один товар → подготовить к выбранным площадкам → опубликовать из одного места.**

Readiness / Compatibility Engine делает этот сценарий работоспособным, а не заменяет его аналитической панелью. Главная ценность: показать причину проблемы, предложить безопасное действие, применить, перепроверить и опубликовать без API Explorer.

- Источники каталога: создание в OmniList и импорт из существующего магазина. По уточнению пользователя первым источником импорта становится подключённый eBay; CSV и Shopify следом. Shopify не является обязательной точкой входа.
- Первые направления публикации: eBay и Etsy. Shopify как направление остаётся в плане, но после этого сквозного сценария. Amazon и другие площадки позже.
- Общий Product хранит факты о товаре. ChannelListing хранит связь и состояние конкретного магазина. Overrides меняют представление для площадки, не исходные факты.
- Начальный publishing-пилот: физические товары без вариантов; eBay EBAY_US/USD. Для проверки двух площадок нужен товар, действительно подходящий обеим, например изделие продавца с подтверждённым происхождением.
- Начать с подходящих продавцов/товаров, а не обещать совместимость любого каталога Shopify с Etsy. Целевой сегмент ещё нужно проверить интервью и пилотом.
- Цена, остаток, изображения и доставка должны быть понятны до publish. ID, payload, templates/recipes и mappings не являются основной формой продавца.
- Заказы, синхронизация, варианты и pricing rules не удалены из исходного scope, но не блокируют исследовательский publishing-пилот. Полноценные общие реальные продажи без согласования остатков небезопасны.
- Не строим сейчас ERP/WMS, рекламу, бухгалтерию, налоги, управление возвратами, сложный repricing и десятки каналов.

## 2. Правила, уточняющие стратегию

- Разделять **готовность данных**, **допустимость товара**, **доступность проверки** и **состояние публикации**. Published не является уровнем readiness.
- Основной UI: «Готово», «Нужно заполнить», «Не поддерживается / не подходит», «Не проверено / проверка недоступна». Процент не означает вероятность успеха; текущий штрафной score не является достоверностью API-проверки.
- Отсутствие блокирующих ошибок разрешает publish даже при необязательных рекомендациях. Не заставлять доводить качество до 100%.
- Readiness относится к версии товара, конкретному подключению, окружению, рынку и версии требований. После изменений результат устаревает. Перед записью сервер перепроверяет критичные условия.
- Не показывать Etsy готовым до реализации и проверки его адаптера. Недоступность API не превращать в пустые требования или успех.
- Не угадывать состояние, изготовителя, происхождение, возраст, GTIN, сертификаты и юридически значимые сведения. Слово Handmade в описании не доказывает «I did».
- Автоисправления сначала только детерминированные, сохраняющие смысл и валидируемые по схеме. Семантические предположения требуют подтверждения; не показывать выдуманные AI confidence 94–99%.
- Issue содержит действие, но клиент не получает право записывать произвольный field/value. Сервер проверяет workspace, актуальность issue и допустимую операцию; повторное применение не должно портить данные.
- Один магазин может завершиться успешно независимо от другого. При таймауте сначала сверка, не слепой повтор создания. Repair/retry входят в P0.
- Inventory sync нельзя отмечать Enabled, пока его нет. Для пилота нужны явные предупреждения об отсутствии синхронизации и ограниченный тестовый остаток.

## 3. Что уже имеется

| Область | Состояние |
| --- | --- |
| Supabase Auth, workspace, каталог и изображения | Реализованы; не требуется переписывать основу |
| Общий товар и overrides | Product имеет явную currency (пилот только USD) и серверную source identity eBay/Trading; SQL 0008 применён и проверен на удалённой БД. Source не принимается из публичного upsert и сохраняется при редактировании |
| Проверки готовности | Readiness API, панель одиночного товара и background publisher (включая bulk execution) используют общий assessment. Bulk preview/каталожные подсказки пока локальные; их нельзя считать полной API-проверкой |
| eBay | OAuth, seller setup, дерево категорий, условия/характеристики, preview и publish одного SKU; два Sandbox-объявления подтверждены пользователем |
| UX eBay | Категории по названию, обязательные характеристики, раскрываемые advanced-поля, уведомления и история; ещё нет единого issue → fix → revalidate |
| Реестр объявлений | ChannelListing, scope подключения/рынка/SKU, внешние ID, последняя успешная версия; SQL 0007 применён |
| Безопасность publish | Локально добавлены claim, needs_review, чтение подключения по ID и проверка сохранённого offer перед записью; расширенные fault/concurrency-тесты и recovery ещё не закончены |
| Очередь | Job/targets хранятся в БД, исполнение пока в процессе API; рестарт не восстанавливает выполнение |
| Etsy / Shopify / CSV | Рабочих интеграций/импорта нет. Типы и preview не считаются готовой интеграцией |
| Просмотр каталога eBay | На `/channels/ebay/catalog` доступны активные объявления Trading API, детали и атомарный одиночный импорт. Уже импортированные/managed объявления получают localLink на Product вместо повторного импорта; браузерная проверка этого дополнения ещё нужна |
| Остатки / заказы / AI | Только ручной остаток и история; синхронизации, заказов и AI resolution нет |

Ключевые точки переиспользования:

- `packages/shared/src/types.ts`, `channel-overrides.ts`: общий товар, overrides и контракты.
- `packages/shared/src/readiness.ts`: базовые проверки. Score сейчас вычитает фиксированные штрафы, не оценивает вероятность публикации.
- `packages/shared/src/ebay-listing.ts`: проверка eBay category requirements; пока возвращает строки.
- `apps/api/src/modules/channels/adapters/ebay-category.ts`: актуальные требования площадки.
- `apps/api/src/modules/validation/assessment.service.ts`: общий assessment; `validation.service.ts` сохраняет re-export базовых правил.
- `apps/api/src/modules/publishing`: адаптеры, реестр связей, jobs и результаты.
- `apps/web/components/ebay-product-fields.tsx`, `publish-product-card.tsx`: существующий UI требований и publish.
- Каталог сейчас на `apps/web/app/page.tsx` (`/`), отдельного `/products` page нет. Workspace товара: `apps/web/app/products/[productId]/page.tsx`.

## 4. P0 — сквозной publishing-пилот

### P0.0. Закончить минимальную безопасность уже начатого publish — В РАБОТЕ

- [x] Общие контракты адаптеров и типизированные внешние ID в job targets.
- [x] Реестр ChannelListing, резервирование scoped SKU и сохранение результата отдельно от истории.
- [x] Применить SQL 0006 и 0007; проверить колонки и индексы 0007.
- [ ] Закончить тесты claim на PostgreSQL: два процесса, сбой БД, таймаут после внешней записи и смена подключения. Имеющиеся in-memory тесты проверяют переходы и конкурирующие вызовы в одном процессе, не закрывают этот gate.
- [ ] Закончить полный repair/recovery: POST `/products/:productId/ebay-offer-repair` только для needs_review с сохранённым offer ID. Проверяет PUBLISHED/ACTIVE, SKU/market/listing ID и подключение; условно снимает блокировку, не подтверждая payload revision. Без ID, при неизвестном статусе и для publishing блокировка сохраняется. Ручной UI добавлен; ещё нужны браузерная проверка, PostgreSQL concurrency и восстановление jobs.
- [x] Уточнить endpoint проверки offer: проверка существования товара в workspace, SKU и marketplace ответа, защита обновления credentials при смене подключения. Endpoint только читает статус; не считать его снятием needs_review или восстановлением задания.
- [ ] Проверить связывание по историческим структурированным ID через eBay API; неоднозначные совпадения не принимать автоматически. Fuzzy matching не нужен в P0.
- [x] Добавить сохранение этапа и известного offer ID до publish. Ошибка checkpoint останавливает следующую внешнюю запись; известный offer сохраняется также в обработчике ошибки, если БД снова доступна. SQL 0009 подготовлен, применение на рабочей БД не подтверждено.
- [ ] Восстановить незавершённое задание после рестарта с проверяемым владельцем выполнения и идентификатором попытки. Нельзя снимать publishing при старте любого API-процесса или только по возрасту записи. Hash payload не заменяет идентификатор попытки. Не нужны отдельные микросервисы.
- [ ] Проверить scoped credentials update, refresh-конкуренцию и замену аккаунта. Чтение по ID уже добавлено, полноценные несколько магазинов ещё не поддержаны UI.

### P0.1. Единая серверная readiness и понятные причины — В РАБОТЕ

- [x] Базовые ValidationIssue и severity существуют; расширять, а не создавать параллельную систему.
- [x] Подключить assessment к GET /products/:productId/readiness. Реализация находится в modules/validation; прежний путь modules/readiness оставлен как совместимый re-export.
- [x] Исправить ложную готовность: недоступные требования/авторизация дают not_checked; отсутствующее подключение/настройки дают needs_attention; неподдерживаемые каналы, рынки и варианты дают not_supported. Необязательные рекомендации не запрещают ready.
- [x] Обновлять токен условно для исходного подключения; отменять результат при смене аккаунта во время проверки. Возвращать checkedAt и revision данных товара/подключения без credentials.
- [x] Объединить общие правила, настройки/авторизацию магазина, актуальные требования категории и ограничения реализации в одном assessment service. (UnifiedAssessmentService в apps/api/src/modules/validation/assessment.service.ts, 2026-09-14 verified)
- [ ] Расширить issue: code/field/severity/message уже есть; title/explanation, resolution type/options и provenance ещё не реализованы.
- [ ] Структурировать category/publish ошибки: неизвестный результат уже даёт not_checked, но строки category validation пока не содержат отдельных resolution/technical details.
- [ ] Завершить общий assessment во всех UI-потоках: single-product UI и background execution подключены, bulk preview/матрица каталога ещё используют локальные проверки.
- [x] Подключить новые статусы к панели одиночного товара: ручная проверка, причины блокировки, ссылки на исправление, рекомендации отдельно. Перед отправкой publish повторять assessment; сбрасывать результат при изменении товара/подключения. (Codex implementation verified)
- [x] Общий тип и runtime-схема assessment в shared; неполный ответ API, чужой product/connection и blocking issue не разрешают публикацию. Readiness response отдаётся с private, no-store.
- [x] Фоновый publisher и bulk используют общий assessment с сохранением защиты непосредственно перед внешней записью. (2026-09-14 verified: publishing.service.ts processTargets() calls assessProduct before claim/publish)
- [ ] Версия требований, устаревание и ограничение параллелизма для каталога. Реализованы только checkedAt и hash товара/подключения/окружения; кеша и версии taxonomy в revision нет.
- [ ] Проверить критерий во всех UI: «Готово» нельзя показывать при неизвестных требованиях/неподдержанном товаре. Общий assessment это обеспечивает; bulk preview и браузерный E2E ещё требуют проверки.

### P0.2. Первый законченный UX на eBay — НЕ ЗАВЕРШЕНО

- [ ] Сценарий: создать → проверить eBay → увидеть только недостающие сведения → исправить здесь же → перепроверить → preview → publish → ссылка/понятное действие.
- [x] Карточка публикации читает серверный assessment и показывает причины; «Исправить здесь» открывает существующий редактор внутри карточки. Сохранение сбрасывает assessment, обновляет страницу и требует новой проверки; настройки магазина пока открываются отдельно.
- [x] Inline-редактор переиспользует category picker, поля состояния и характеристик. Нового параллельного редактора и API ID формы нет.
- [x] После успешной проверки доступен отдельный preview с актуальными eBay overrides, фотографиями, ценой, остатком, категорией и состоянием. Только явное подтверждение запускает publish; серверная проверка перед записью сохранена.
- [ ] Добавить Apply Fix для ограниченного набора серверных операций над overrides; проверка версии, разрешений и повторного применения.
- [ ] Начать без LLM: формат/пустые значения, достоверные существующие атрибуты и выбор из схемы. Не подменять факт безопасно выглядящей догадкой.
- [ ] Перестроить product workspace в общие сведения и панель площадки; создать переиспользуемую issue card, не универсальный конструктор произвольных форм.
- [ ] Матрица каталога показывает состояние проверки и количество проблем, а рядом независимо состояние объявления. Не копировать демонстрационные проценты стратегии.
- [ ] После исправления повторять только неуспешную цель; для неопределённой публикации сначала кнопка проверки результата.
- [ ] Проверить мобильный интерфейс, клавиатуру, ошибки сети и сценарий нового продавца без API Explorer.

### P0.3. Etsy как второе направление — НЕ НАЧАТО

- [ ] Параллельно P0.1/P0.2 получить нужный доступ Etsy API и подходящий магазин; подтвердить ограничения приложения для пилота и публичного сервиса.
- [ ] OAuth PKCE, одноразовая попытка, минимальные scopes, безопасный refresh.
- [ ] Eligibility: происхождение/участие продавца, тип товара и применимые ограничения; неподходящий товар блокируется, не «чинится» до Handmade.
- [ ] Загрузить taxonomy/properties, доставку, возвраты и processing profiles; дать выбор по названиям.
- [ ] Подключить assessment и resolution к тем же UI-контрактам без копирования eBay-логики.
- [ ] Draft → сохранить listing ID → изображения/характеристики → activation → проверка; учитывать платность и доступные тестовые возможности.
- [ ] Сквозной тест подходящего товара: eBay успешен, Etsy требует исправления, retry Etsy не перепубликует eBay.

### P0.4. Импорт существующего каталога — ТЕКУЩИЙ ПРОДУКТОВЫЙ СРЕЗ

- [x] Native create остаётся самостоятельным основным сценарием.
- [x] Первый read-only этап: просмотр Inventory API товаров подключённого eBay, страницы по 20 записей, явное окружение и предупреждение об ограниченном охвате. Без записи товаров/объявлений, без автоматического переноса в Inventory API.
- [x] Ошибка/неполный ответ не превращается в пустой каталог. Проверяются workspace, подключение и его изменение во время запроса; credentials не возвращаются клиенту.
- [x] Подключить GetMyeBaySelling к API и текущему catalog preview: активные объявления, страницы 1-125, явное окружение и ограничения, цены/ссылки; проверка подключения после ответа eBay, runtime-схема на клиенте. GetItem подключён отдельным read-only этапом ниже; импорт ещё не реализован.
- [x] GetItem details preview: кнопка «Подробнее» у активного объявления, актуальная проверка принадлежности через GetMyeBaySelling, проверка connection/environment до запроса и подключения после ответа. Категория, состояние, Money без конвертации, Quantity минус QuantitySold, характеристики и ограничения вариантов/аукционов. Общая runtime-схема `EbayListingDetails`; source identity и checkedAt. `importAvailable` рассчитывается по подтверждённым данным; сырой HTML, seller PII и токены не возвращаются клиенту.
- [x] Связать preview с импортом одного объявления в общий Product: кнопка доступна только после серверной проверки деталей, показывает loading/error и результат imported/existing/managed со ссылкой на Product. Используются точные внешние ID, окружение, подключение и валюта; по названию или одному совпадению SKU товары не сливаются.
- [x] Внутренний PostgreSQL repository импорта: одна вставка Product+source в транзакции, блокировки workspace/connection, проверка актуальности подключения, точный поиск уже импортированного/управляемого listing ID, отказ при неоднозначной связи/незавершённой публикации. Обычное создание Product и eBay claim используют тот же workspace lock.
- [x] Атомарно сохранить Product и source/listing identity; повтор/параллельный импорт не создаёт дубли, сбой не оставляет сирот. Лимит тарифа и уже опубликованные из OmniList товары проверены на PostgreSQL.
- [ ] Существующее объявление показывать как связанное, не предлагать создать его повторно. Для Trading-origin явно разделять чтение и возможность дальнейшего управления; никаких скрытых миграций.
- [x] Минимальная локальная модель currency + source: environment/listingId/connectionId/sellerId/api/importedAt/currency. Связь хранится в той же строке Product, уникальность источника в пределах workspace по channel/environment/listingId. Изменение источника через upsert запрещено; source не является разрешением писать в Inventory API. SQL 0008 применён; исходные данные четырёх товаров сохранены без изменений.
- [ ] Более подробный provenance и правила обновления отдельных полей источником остаются следующими задачами; сейчас повторный импорт не перезаписывает локальные правки.
- [ ] CSV: sample, preview, ошибки по строкам, явные валюты/варианты, dry run, детерминированные правила повторного импорта и дублей.
- [ ] Shopify OAuth для импорта: development store, нужные scopes, источники product/variant ID, курсор/возобновление и лимиты.
- [ ] Импорт идёт в тот же Product и тот же assessment. Не перезаписывать локальные правки или overrides без явного правила владения данными.
- [ ] Onboarding предлагает Create / eBay / CSV / Shopify только по фактической доступности функций. В каталоге eBay доступен одиночный импорт; CSV/Shopify нельзя показывать как доступные источники.

## 5. P1 — расширение доказанного сценария

- [ ] Bulk safe fixes с preview, подтверждением семантических изменений и повторной проверкой.
- [ ] Existing listing reconciliation: точные внешние ID/SKU, затем вероятные совпадения; пользователь подтверждает fuzzy/image/variant matches, без автоматического слияния по похожему названию.
- [ ] AI category candidates / extraction с источником предложения и подтверждением; измерять качество до отображения confidence. Сохранять историю принятых/отклонённых mappings и версию taxonomy.
- [ ] Варианты: реальное соответствие внешних вариантов, а не только поля в Product. Отдельные acceptance-тесты по площадкам.
- [ ] Shopify как направление publish: сохранить product/variant/inventory IDs, выбрать location/publication, проверить создание/обновление без дублей.
- [ ] Pricing rules и округление поверх явного Money; никакой скрытой конвертации валюты.
- [ ] Операции каталога: поиск, фильтры, пагинация, копирование/архив и bulk editing. Вес/габариты/GTIN добавлять по потребностям поддержанных товаров и площадок.

## 6. P2 — эксплуатационный многоканальный продукт

- [ ] Источник истины остатков, атомарные корректировки, резервы/распределение и политика конфликтов.
- [ ] Заказы с внешними ID, deduplication, webhooks/polling и повторная сверка.
- [ ] Реальные stock updates между магазинами, last-sync visibility, нулевой остаток и одновременные продажи. Не обещать отсутствие oversell без проверки.
- [ ] Единый список заказов; затем дополнительная операционная аналитика.
- [ ] Amazon после доказательства engine; WooCommerce, Walmart и региональные каналы после проверки официального доступа.
- [ ] Оплата подписки и server-side entitlement перед платным запуском; тарифы проверять на пилоте, не фиксировать по выдуманным ценам конкурентов.

## 7. Поперечные release-gates

- [ ] Безопасность: encryption credentials, refresh serialization, workspace isolation, одноразовый OAuth/PKCE, очистка логов, media SSRF и подписи webhook.
- [ ] Согласовать фактическую Supabase schema, журнал Drizzle и snapshots; проверить миграции на пустой/существующей БД и восстановление backup.
- [ ] Подтвердить production build, CI, деплой/откат и модель фонового выполнения при сне Render.
- [ ] Метрики: время до первой публикации, first-attempt success, причины отказов, время до исправления, доля принятых/отменённых предложений. Не повышать readiness скрытием ошибок.
- [ ] Проверить продукт на нескольких продавцах и типах товаров, а не одной футболке. Сравнить число ручных действий и ошибок до/после.
- [ ] До публичного брендинга проверить похожие названия, домены и права; численные threat scores из исследования в backlog не переносить.

## 8. Условия готовности и проверенные факты

**Первый продуктовый срез:** продавец проходит eBay issue → fix → revalidate → preview → publish без API Explorer; неизвестная ошибка не выглядит успехом, повтор не создаёт дубль, нет требования заполнить рекомендации ради 100%.

**Publishing-пилот двух каналов:** товар подходит eBay/Etsy, есть отдельные реальные результаты и безопасный repair; ограничения отсутствующей синхронизации явно обозначены. Это не полный исходный MVP.

**Полный исходный MVP:** дополнительно импорт, варианты, основные операции каталога, согласованные остатки/заказы, pricing и согласованный AI scope; production/безопасность подтверждены тестами.

- 2026-09-13: текущие 34 теста выполнены через in-process harness, shared/API/web typecheck без ошибок. Это не PostgreSQL concurrency test и не полная Next production build. Новые ветки восстановления/claim нуждаются в дополнительных тестах.
- 2026-09-13, catalog preview: 39/39 тестов пройдены через in-process harness (5 новых); shared/API/web typecheck без ошибок. Проверены страницы, пустой/неполный ответ, отказ API/сети, workspace/environment isolation и disconnect во время чтения. Новая миграция не требуется. Реальный eBay E2E, браузерная проверка и production build этого этапа не выполнены; деплой не выполнялся.
- 2026-09-14, исправления после ревью Claude: 45/45 тестов через in-process harness, shared/API/web typecheck без ошибок. Новые проверки покрывают сбой авторизации/категории, неподдерживаемые каналы, настройки магазина, refresh, смену аккаунта, неверное состояние и нессовпадающий offer. SQL-миграций нет. Реальный eBay E2E, production build и деплой не выполнялись; P0.0/P0.1 не объявлены завершёнными.
- 2026-09-14 (работа Claude, уточнено после ревью): добавлены 6 in-memory тестов состояний и Trading endpoint. Первоначальный repair был неработоспособен, а закрытие P0.0/P0.1 преждевременно. Старый результат 17/17 не подтверждает PostgreSQL concurrency, полный assessment UX или recovery.
- 2026-09-14, assessment UI: 46/46 тестов через in-process harness, shared/API/web typecheck без ошибок. Дополнительно проверено запрещение публикации при неполном/противоречивом assessment. Браузерный E2E и production build не выполнены; деплоя и SQL-миграций нет.
- 2026-09-14, recovery + active catalog preview: 66/66 тестов через in-process harness; shared/API/web typecheck без ошибок. Добавлены 8 тестов recovery/active catalog и тест отказа assessment до claim с успешным retry. SQL-ветка recovery использует условный UPDATE, но на PostgreSQL не проверена. GET больше не записывает реестр. Endpoint active-listings сохраняет плоский контракт ответа. Миграций, деплоя, реального eBay E2E и браузерной проверки не было; production build/Git CLI блокируются Windows CET.
- 2026-09-14, GetItem preview: 75/75 тестов через in-process harness, shared/API/web typecheck без ошибок. Девять новых тестов проверяют парсинг, отсутствие догадок, точную source identity, ownership до GetItem, смену аккаунта, ограничения и ограниченный транспорт (таймаут 15 с, 2 МБ, запрет redirects). Никаких Product/ChannelListing записей, миграций и деплоя. Реальный eBay E2E, браузерный E2E и production build не выполнены; shell по-прежнему падает с Windows CET. Это просмотр деталей, не готовый импорт.
- 2026-09-14, persistence foundation: 84/84 тестов через in-process harness; shared/API/web typecheck и отдельный typecheck нового теста без ошибок. Девять новых тестов: импорт/повтор/managed listing/лимит/отказ snapshot/rollback/public-source forgery/currency/publish guard. Транзакции проверены тестовым двойником, НЕ реальным PostgreSQL или несколькими процессами. SQL 0008 и журнал подготовлены, миграция НЕ применена; API import не подключён. Native editing/stock updates сохраняют currency. Импортированные Trading товары блокируются базовой readiness, assessment и direct eBay adapter. Нет деплоя/production build/browser E2E.
- SQL 0008 применён отдельно в транзакции на настроенной удалённой БД: products.currency/source и уникальный индекс проверены, все четыре прежних товара сохранены (сверена контрольная сумма всех прежних полей). USD назначен без конвертации; у настроенного eBay-пилота EBAY_US/USD, признаков другой валюты не найдено. Историческая цепочка и фиктивный baseline на рабочей БД не запускались.
- Пользователь подтвердил два eBay Sandbox-объявления; новые локальные изменения и production-публикация не проверены на деплое.
- 0006 применён 2026-09-12. 0007 применён 2026-09-13 в транзакции: проверены 13 колонок и 3 уникальных индекса, включая primary key. Существующие данные не менялись.
- У удалённой БД не было `drizzle.__drizzle_migrations`. Не прогонять старые create-миграции поверх неё без baseline. Claim использует существующую varchar status, новой SQL-миграции для этих значений нет.
- Shell ранее падал из-за Windows CET до запуска команды; git status и полноценная shell-сборка не подтверждены.
- Реализация claim/offer verification начата до пересмотра стратегии и не объявляется завершённой по одному зелёному старому набору тестов. Не деплоить её без P0.0 проверки и repair-пути.

### Проверка PostgreSQL persistence и server import (2026-09-15)

- [x] Девять интеграционных сценариев выполнены на реальном PostgreSQL в отдельной случайной схеме; тестовые таблицы и функции удалены после выполнения. Рабочие таблицы в этих тестах не использовались.
- [x] Проверены SQL 0000–0008 на пустой изолированной схеме (явные public FK перенаправлены только в тестовую схему), upgrade существующей строки и rollback DDL 0008. Это не baseline удалённого Drizzle и не проверка восстановления backup.
- [x] Два независимых backend-соединения действительно одновременно ожидали workspace lock: повторный импорт оставил один Product, native create/import не превысили лимит 10. Оба порядка import/native publish не создают конкурирующие identity; claim повторно проверяет подключение под блокировкой. Проверены уникальный индекс при обходе repository, разделение окружений, rollback после INSERT, повтор после сбоя, сохранение локальных правок и возврат managed listing.
- [x] Исправлен выявленный интеграционными тестами ложный отказ при смене порядка JSONB-ключей metadata: сравнение содержимого вместо JSON.stringify. Изменение значений/статуса подключения по-прежнему блокирует импорт.
- [x] Добавлен `POST /channels/ebay/active-listings/:listingId/import`: body содержит только page/connection/environment. Сервер повторно проверяет ownership через GetMyeBaySelling, читает GetItem и формирует Product сам. HTML описания сохраняется как plain text; разрешены только HTTPS-фотографии eBay CDN; обязательные SKU/цена/остаток/описание не угадываются. Поддержаны только active FixedPriceItem без вариантов, eBay US/USD.
- [x] UI одиночного импорта встроен в проверенные детали объявления. Неподдерживаемые объявления показывают причины без кнопки; результат imported/existing/managed использует общий flash и ссылку на Product. Старая неподключённая bulk-заготовка не используется.
- 88/88 обычных тестов через in-process harness; shared/API/web и все тестовые файлы проходят TypeScript-проверку. PostgreSQL: 9/9 сценариев с реальными SQL, блокировками и транзакциями, не mock DB. Shell всё ещё блокируется Windows CET; production build, браузерный E2E и реальный вызов нового import endpoint в этом этапе не выполнялись.
- Повторный запуск: `TEST_DATABASE_URL` для выделенной тестовой БД (direct/session connection, право CREATE SCHEMA), затем `npm run test:postgres`. Без переменной тест явно пропускается; fallback на DATABASE_URL нет. Тест исключён из обычного `npm test`.
- Пользователь сообщил о деплое и ручном тестировании предыдущего этапа; точный набор проверенных сценариев не указан. Текущие claim/import изменения ещё требуют деплоя. HTTP import подключён, UI импорта ещё нет.

### Безопасные границы recovery (2026-09-15)

- Удалён опасный автоматический recovery при старте API: он мог снять claim ещё работающей публикации другого процесса. Удалено массовое завершение jobs по совпадению product/connection без привязки к конкретной попытке.
- Repair работает только с needs_review и сохранённым offerId. Нет поиска и присвоения offer по одному SKU. Publishing, чужой аккаунт, несовпадающий listingId и неизвестный статус остаются заблокированными.
- Подтверждённый активный offer восстанавливает связь, но не доказывает успешность последнего payload: appliedRevision остаётся null. Подтверждённый unpublished offer без listingId разрешает повтор с тем же offerId.
- Тестовое хранилище и PostgreSQL одинаково отклоняют результат с несовпадающей attemptRevision. Добавлены регрессии checkpoint, отсутствия publish при сбое сохранения, сохранения известного offer при обработке ошибки и запрета recovery работающей публикации.
- Обычный `npm test` теперь запускается через CLI: scripts/package.json задаёт ESM для тестов. Проверено 94/94 теста и typecheck API/web/shared. PowerShell блокируется CET, cmd.exe работает.
- PostgreSQL-сценарий расширен SQL 0009 и проверками checkpoint/recovery, но текущий запуск пропущен: TEST_DATABASE_URL не задан. Рабочая БД, миграции и деплой в этом подэтапе не изменялись. Browser/eBay E2E не запускались. P0.0 и P0.2 не закрыты.

## 9. Как поддерживаем план

### Ручной repair и переход к объявлению (2026-09-15)

- [x] На странице товара после неудачной eBay-публикации доступен раскрываемый блок «Результат публикации неизвестен?». Пока job queued/processing, действие не показывается. POST проверяет только сохранённый offer; никаких автоматических create/publish/retry.
- [x] UI различает подтверждённое активное объявление и неопубликованный offer, разрешённый к повтору. Не выдаёт восстановление связи за подтверждение последних правок или завершение старого job. Ошибка/409 остаётся видимой, повторная проверка доступна.
- [x] Runtime-контракт проверяет product/connection и согласованность результата. Повторные клики блокируются; запрос отменяется при размонтировании. EN/RU/UK, aria-live и явные сообщения результата.
- [x] Успешные eBay targets в истории получили ссылку «Открыть объявление». URL строится из типизированных environment/listingId, не из произвольного ответа; Sandbox ведёт на sandbox.ebay.com.
- Проверено: 96/96 unit/regression tests, API/web/shared typecheck. Это не браузерный E2E. Новых SQL-миграций, вызовов живого eBay и деплоя нет; ранее подготовленная SQL 0009 по-прежнему требует отдельной проверки применения.
- Следующий пользовательский срез: P0.2, исправление проблем прямо в workspace товара с повторной проверкой и preview. Автоматическое восстановление после рестарта остаётся открытым P0.0 release-gate; ручной repair не заменяет его и не разблокирует зависший publishing.

Перед работой читать этот файл и затрагиваемый код. После подэтапа обновлять статус, проверки, миграции и deployment отдельно. Не создавать дополнительные status/roadmap MD.

**Следующий шаг:** P0.0: добавить проверяемого владельца job/attempt и безопасное обнаружение завершившегося процесса; связать восстановление сохранённого offer с конкретным job target. Проверить рестарт, два одновременно работающих API-процесса и потерю ответа после внешней записи на PostgreSQL. Затем P0.2: inline issue → fix → recheck → preview → publish → listing. Для уже добавленного localLink в каталоге отдельно проверить браузерный сценарий details → Import → Product и повтор без дубля; массовый импорт пока не расширять.

### Inline correction и preview (следующий eBay-срез)

- Реализован путь issue → существующий редактор на месте → save → новая проверка → preview → подтверждение publish. Закрытие редактора не сохраняет изменения и не возвращает старую готовность. Пока показан полный редактор, а не только поля конкретного issue; scoped Apply Fix с проверкой версии остаётся открытым.
- ProductEditor проверяет runtime-схему ответа и ID редактируемого товара. Режим самостоятельной страницы сохранён, embedded-режим использует callback без навигации на другой URL.
- Подтверждено: production `npm run build:web`, typecheck, 96/96 существующих regression tests. Эти тесты не покрывают браузерные взаимодействия нового редактора. Browser/mobile E2E и проверка eBay Sandbox не выполнены; деплоя, новых миграций и изменений рабочей БД нет.
- Порядок завершения eBay: (1) версия/конкурентное сохранение и targeted fixes; (2) незавершённые jobs, безопасный retry и PostgreSQL-тесты; (3) browser/mobile E2E create/import → fix → preview → publish/update → listing на Sandbox; (4) проверка ограничений пилота и deployment. Не считать все eBay-задачи закрытыми по сборке.
- После этого Etsy как второе направление, затем Shopify. Для реального E2E понадобятся доступ приложения и тестовый магазин каждой площадки. До проверки адаптеров UI по-прежнему не должен разрешать публикацию в эти каналы.

### Защита сохранения товара (2026-09-16)

- [x] Сервер отдаёт revision содержимого Product, включая остаток, source и overrides. Порядок JSON-ключей не влияет на revision; клиент не может записать его через upsert. SQL-миграция для этого не нужна.
- [x] Публичный PUT требует If-Match: без версии 428, при устаревших данных 409. PostgreSQL проверяет версию под блокировкой строки в той же транзакции, что и UPDATE. Проверка до media upload отсеивает уже известный конфликт, проверка внутри repository закрывает гонку после upload.
- [x] Редактор и веб-прокси передают If-Match. При конфликте форма сохраняет локальные правки, объясняет необходимость сравнить их с актуальным товаром и блокирует повторную перезапись. Автоматического merge или обновления версии под старой формой нет.
- [x] Внутреннее изменение остатка передаёт исходную revision, чтобы не затирать параллельное редактирование. Атомарность записи stock movement вместе с Product остаётся отдельной задачей.
- Проверено: 99/99 unit/regression tests, typecheck API/web/shared, production build:web. Добавлен PostgreSQL-тест двух одновременно ожидающих редакторов; запуск пропущен без TEST_DATABASE_URL. Не заявлять SQL-конкуренцию проверенной этим запуском. Browser/Sandbox E2E и деплой не выполнялись.
- API и web нужно деплоить согласованно: старые открытые формы без revision должны перезагрузиться; сервер намеренно не принимает безусловный PUT. Данный срез не закрывает защиту версии между preview и выполнением фоновой публикации, targeted Apply Fix или recovery jobs.

**Ближайший порядок завершения eBay:** привязать preview/publish к версии товара; закончить recovery конкретного job с защитой живого исполнения; выполнить PostgreSQL и browser/Sandbox release-gates. После этого переходить к Etsy, затем Shopify. Локальный inline-редактор и ссылки на listing уже добавлены, но весь eBay-блок пока не объявляется готовым.

### Привязка preview к публикации (2026-09-16)

- [x] Readiness возвращает productRevision. Панель сверяет её с товаром, показанным в preview; при расхождении сбрасывает готовность и предлагает обновить товар.
- [x] Одиночный POST publish требует productRevision. Enqueue сравнивает подтверждённую версию с серверным снимком: конфликт 409 не создаёт job и не вызывает eBay. Старые клиенты без версии получают ошибку валидации; web/API деплоить согласованно.
- [x] Принятое задание использует глубокую копию подтверждённого товара, включая вложенные overrides. Редактирование после принятия задания не подменяет его содержимое. Это snapshot-семантика: уже принятое задание не отменяется последующей правкой товара.
- Проверено: 101/101 тест, включая отказ до enqueue и изменение вложенных полей после принятия; API/web/shared typecheck. Повторный production build не запущен: сервис проверки разрешения недоступен. Новых миграций и деплоя нет. Browser/Sandbox E2E не выполнены.
- Ограничения: снимок пока в памяти, не в БД; restart recovery не закрыт. Bulk endpoint пока работает по текущему серверному товару, без такого подтверждения preview. Версия настроек подключения в подтверждение клиента ещё не включена; серверные проверки подключения сохраняются.
- **Следующий шаг:** durable job snapshot и идентификатор владельца попытки, восстановление именно её результата без разблокировки живого процесса; затем PostgreSQL-конкуренция и Sandbox/browser E2E. Etsy/Shopify до этих проверок не включать в доступные publish-каналы.

### Сохранённый снимок задания (2026-09-16)

- [x] Подготовлена SQL 0010: publish_jobs.product_snapshot (nullable JSONB). Новые jobs сохраняют проверенный Product с revision в одной транзакции с targets. Старые jobs остаются без снимка; backfill из текущего товара запрещён.
- [x] Исполнитель читает сохранённый snapshot перед обработкой, не использует изменяемый каталог. При отсутствии/повреждении снимка нет fallback на текущий Product и нет вызова eBay. Обновление job/targets не меняет snapshot.
- [x] Snapshot доступен только через внутренний workspace-scoped repository method, не включён в публичную историю jobs. Runtime-схема исключает посторонние поля; credentials подключения в snapshot не сохраняются. Memory repository возвращает копии, чтобы вызывающий код не менял сохранённые данные по ссылке.
- Проверено: 104 unit/regression tests, typecheck API/web/shared и build:api. PostgreSQL-тест добавлен для legacy job, чтения через другой repository, редактирования исходного Product, workspace isolation и неизменности snapshot после updateJob; запуск пропущен без TEST_DATABASE_URL.
- SQL 0010 НЕ применён к рабочей БД, деплоя нет. Перед деплоем этого кода проверить и применить подготовленные 0009/0010 с учётом отсутствовавшего baseline; не запускать всю историческую цепочку вслепую.
- **Остаётся P0.0:** владелец/идентификатор исполнения и безопасное восстановление конкретного job. Сохранённый snapshot необходим для recovery, но сам по себе не делает очередь восстанавливаемой. Автоматический restart recovery по-прежнему выключен.

### Применение миграций и атомарный старт job (2026-09-16)

- [x] По запросу пользователя SQL 0009 и 0010 применены к настроенной удалённой БД одной транзакцией. До запуска проверено отсутствие новых колонок и журнала Drizzle; историческая цепочка и фиктивный baseline не запускались.
- [x] Под блокировкой записей сравнены контрольные суммы прежних полей: все 4 Product, 1 ChannelListing и 25 jobs сохранены без изменений. Проверены четыре новые колонки, индекс channel_listing_recovery_status и отсутствие backfill snapshot у старых jobs. Это supersedes прежние записи «0009/0010 не применены».
- [x] Repository получил атомарный claimJob: только queued → processing, вместе со статусами targets в одной SQL-транзакции. Конкурирующий запуск или чужой workspace не получает задание. Исполнитель сначала получает claim; неудачный/неопределённый claim не позволяет записать failed поверх чужой работы.
- Проверено: 105/105 тестов, typecheck API/web/shared. В PostgreSQL integration suite добавлена гонка двух repository при claim; сам этот тест в данном этапе не запускался. Применение DDL на рабочей БД не заменяет SQL concurrency test.
- Деплой приложения и browser/Sandbox E2E не выполнялись. Следующий шаг: идентичность владельца попытки и безопасная сверка прерванного processing, затем live release-gates. claimJob предотвращает двойной старт, но пока не восстанавливает processing после падения процесса.

Уточнение 2026-09-15: background publisher/bulk execution использует assessment перед claim. GET offer status только читает; POST recovery восстанавливает лишь подтверждённый сохранённый offer и не изменяет историю jobs. Trading import создаёт read-only source Product и не мигрирует объявление в Inventory API. Заготовка `ebay-import-listings.tsx` по-прежнему не подключена и не соответствует новому одиночному endpoint; её нельзя считать готовым UI.

Ограничения импорта проверять по официальным API: [управление и чтение объявлений eBay](https://developer.ebay.com/develop/guides/sell/listing-management), [почему Inventory API не видит другие объявления](https://www.developer.ebay.com/support/knowledge-base/5210). Preview читает только inventory records, не подтверждает цену или активную публикацию.

Проверенные внешние ориентиры: [правила допустимости Etsy](https://www.etsy.com/legal/creativity/), [доступ Etsy API](https://developers.etsy.com/), [Nembol Checkup](https://help.nembol.com/nembol-settings-and-functions/nembol-functions/checkup/). Наличие Checkup подтверждено; утверждение об отсутствии аналогичных возможностей у конкурентов требует отдельной проверки, не считается доказанным.

GetItem reference: [детали объявления, ItemSpecifics и расчёт доступного остатка](https://developer.ebay.com/devzone/xml/docs/Reference/ebay/GetItem.html). Проверка деталей не подтверждает готовность к публикации и не разрешает управлять Trading-origin объявлением через Inventory API.
